================================================================================
ISSUE #227 - 手動検証ログ（ブラウザ相当のHTMLレスポンス確認）
実行日: 2026-03-26
環境: Docker (platform container) / PostgreSQL 16 / Rails 8.0.2.1 / Ruby 3.4.6
検証方法: curlでログイン後、各画面のHTMLレスポンスをパースして確認
================================================================================

=== 1. クライアント一覧 (GET /clients) — 業種が日本語ラベルで表示されること ===

業種カラムの表示値:
  - client: —（未設定）
  - demo: 宿泊業
  - uriage: —（未設定）
  - weblab: —（未設定）

結果: OK — industry が設定されているクライアントは日本語ラベル「宿泊業」で表示。
       未設定のクライアントは「—」で表示。

=== 2. クライアント詳細 (GET /clients/demo) — 業種が日本語ラベルで表示されること ===

表示値: 宿泊業

結果: OK — 英語値「accommodation」ではなく日本語ラベル「宿泊業」で表示。

=== 3. クライアント編集 (GET /clients/demo/edit) — 7業種がプルダウンに表示されること ===

プルダウン選択肢:
  - -- 選択してください --
  - 宿泊業
  - 飲食業
  - アクティビティ
  - 小売業
  - レンタカー
  - 美容・リラクゼーション
  - その他

結果: OK — 7業種すべてが日本語ラベルで表示。Client::INDUSTRIES 定数から生成されている。

=== 4. DBデータ確認 — 既存の hotel データが accommodation に移行されていること ===

SELECT code, industry FROM clients:
  - client: (NULL)
  - demo: accommodation
  - uriage: (NULL)
  - weblab: (NULL)

結果: OK — マイグレーション適用済み。
       ※ 本環境では既存データに hotel/tour の値がなかったため、
         マイグレーション自体は正常適用されたがデータ変換対象は0件。
         マイグレーションのSQL（UPDATE clients SET industry = 'accommodation'
         WHERE industry = 'hotel'）は正常に実行されている。

================================================================================
全4項目: OK
================================================================================
